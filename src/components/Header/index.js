import Spinner from 'react-bootstrap/Spinner';

export const Header = () => {
    return (
      <>
      <Spinner animation="grow" variant="primary" />

        SPC Zone Monitor
      </>
    );
  };