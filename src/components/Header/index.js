import { Head } from "./styles"
import Emoji from "../Emoji";

export const Header = () => {
    return (
      <>
      <Head><Emoji symbol="🚨 " label="alarm"/>SPC Zone Monitor</Head>
      </>
    );
  };